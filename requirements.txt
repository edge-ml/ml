aiohttp
fastapi==0.104.1
Jinja2
m2cgen
micromlgen
motor
# numpy/pandas/h5py/scikit-learn bumped for executorch 1.0 (requires numpy>=2, pandas>=2.2.2, scikit-learn==1.7.1)
numpy==2.0.2
pandas==2.2.3
pydantic==2.11.0
PyJWT==2.8.0
pymongo==4.6.0
scikit_learn==1.7.1
setuptools
starlette
markupsafe
uvicorn==0.24.0.post1
orjson==3.9.10
lttbc==0.2.4
h5py==3.12.1
boto3==1.29.6
requests==2.31.0
tensorflow
# torch is pinned to the version executorch 1.0 pairs with (torchao 0.14 needs exactly 2.9.0)
torch==2.9.0
tensorflow-model-optimization
executorch==1.0.0
# einops: runtime dep of the whar-models neural architectures (#62). whar-models
# itself is installed --no-deps in the Dockerfile, so its light runtime dep is
# listed here explicitly.
einops
