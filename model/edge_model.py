class EdgeModel:
    def __init__(self):
        self.is_fit = False

    def get_hyperparameters():
        return {
            window_size: {
                parameter_type: 'number',
                number_min: 0,
                number_max: 5,
                inclusive_min: False,
                inclusive_max: True,
                precision: 'int',
                required: True
            }
        }

    def fit(X, y):
        return
    
    def predict(X):
        return

    def compile():
        if (not self.is_fit):
            return
            # TODO: throw error
        
