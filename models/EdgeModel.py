class EdgeModel:
    @staticmethod
    def get_hyperparameters():
        return None # todo: parameter builder

    def __init__(self, hyperparameters):
        self.name
        self.is_fit = False
        
    def fit(X, y):
        return
    
    def predict(X):
        return

    def compile():
        if (not self.is_fit):
            return
            # TODO: throw error
        
